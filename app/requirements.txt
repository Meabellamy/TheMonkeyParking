pyserial>=3.5

# La interfaz visual usa Tkinter, que viene incluido de fabrica con
# Python (no requiere instalar nada mas). Se eligio Tkinter en vez de
# una libreria grafica externa (como pygame) para evitar problemas de
# antivirus / Smart App Control de Windows bloqueando DLLs de terceros,
# y para que la instalacion sea lo mas simple posible en cualquier PC.
